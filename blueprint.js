/** 
 * !     Clipping tools module
 * 
 * !      New:
 *        * /clip-live <channel> <?duration> <?title>
 *  TODO:    - Makes a clip with current live timestamp and queues it up for download.
 * 
 *        * >mc <recent>
 *  TODO:    - Shows user stats and recent clips
 * 
 *        * >recent <page_number>
 *  TODO:    - Shows recent clips made using commands
 * !      ---
 * 
 *        * /clip <channel> <timestamp> <?duration> <?title>
 *  TODO:    - makes a clip from specified channel that is live and uploads it to streamable.com (last available vod is clipped)
 * 
 *        * /clip-vod <vod_id> <timestamp> <?duration> <?title>
 *  TODO:    - makes a clip from specified VOD and uploads it to streamable.com
 * 
 *        * /clip-save <streamable_link>
 *  TODO:    - links a download url for streamable
 * 
 *        * /timestamp-live <channel> <?title>
 *  TODO:    - generates a vod url with current timestamp
 * 
 *        * /timestamp <channel> <timestamp> <?title>
 *  TODO:    - generates a vod url with specified timestamp
 * 
 *        * /sync-live <channel> <?title>
 *  TODO:    - sends a bot message in #general with current live timestamp, makes a hyperlink in a bot channel for a quick access (unofficial pin)
 * 
 * !         SYNC COMMAND COMING TOMORROW :copium:
 *        * /sync <message_id> <channel> <?title>
 *  TODO:    - same as above but uses specified message_id instead of sending new bot message
 * 
 * !  
*/